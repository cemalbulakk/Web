@model ProductListModel

<div class="row">
    @*<div class="col-md-3">
       @await Component.InvokeAsync("CategoryList")
    </div>*@
    <div class="col-md-12">
        @*<partial name="_jumbotron" />*@
        <partial name="_slider" />

        <div class="row">
            @foreach (var product in Model.Products)
            {
                <partial name="_product" for="@product" />
            }
        </div>

    </div>
</div>


